# Dépendances d'exécution.
# Bornes hautes volontairement strictes : un `pip install -U` ne doit jamais
# faire sauter le projet sur une version majeure non testée sans revue.
requests>=2.31,<3
openpyxl>=3.1,<4
Pillow>=10,<13

# Outils de développement : py -m pip install -e ".[dev]"
